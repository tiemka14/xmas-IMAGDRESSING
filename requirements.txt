fastapi==0.110.0
uvicorn==0.29.0
torch==2.1.0
torchvision==0.16.0
Pillow
accelerate
transformers
diffusers
# VITON-HD requirements
opencv-python
torchgeometry
kornia
# The VITON-HD repository is cloned and installed during the Docker build. Keep any upstream git pins commented
# if you want to transform into a proper package in the future.
# (Previously used IMAGDressing git link; switched to VITON-HD and local adapter)
